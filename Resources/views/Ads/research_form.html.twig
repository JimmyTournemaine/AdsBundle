	{{ form_start(research_form, {'attr': {'class': 'form-inline'}}) }}
	
	
	{{ form_widget(research_form.keywords) }}
	{{ form_widget(research_form.minPrice) }}
	{{ form_widget(research_form.maxPrice) }}
	{{ form_widget(research_form.sortBy) }}
	<input type="submit" value="Rechercher" class="btn btn-primary" />
	{{ form_end(research_form) }}
	
	<hr />
